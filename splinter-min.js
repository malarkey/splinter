<script>initTextSplitting('[data-split="toon"]');function initTextSplitting(e){const t=document.querySelectorAll(e);if(0===t.length)return console.log(`No elements found for selector:"${e}"`);console.log(`Applying toon text to ${t.length} element(s)`),t.forEach(e=>{e.setAttribute('aria-label',e.textContent),e.innerHTML.includes('<br>')?e.innerHTML=e.innerHTML.split('<br>').map(e=>`<span class="toon-line">${e.trim()}</span>`).join('<br>'):e.innerHTML=`<span class="toon-line">${e.innerHTML}</span>`}),toonTextSplitter('.toon-line')}function toonTextSplitter(e){const t=document.querySelectorAll(e);let o=0;t.forEach(e=>{const t=e.textContent;let n='';for(let e=0;e<t.length;e++){const c=t[e];' '===c?n+=' ':(n+=`<span class="toon-char" aria-hidden="true">${c}</span>`,o++)}e.innerHTML=n}),document.querySelectorAll('.toon-char').forEach(e=>{e.style.display='inline-block'}),console.log(`Split into ${o} characters`)}</script>
